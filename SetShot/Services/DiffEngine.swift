import Foundation

struct DiffEngine {
    func diff(before: Snapshot, after: Snapshot, kb: KnowledgeBase) -> DiffResult {
        // TODO: implemented in DiffEngine session
        return .empty
    }
}
